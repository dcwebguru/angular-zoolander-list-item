'use strict';

describe('myApp.viewZoo module', function() {

  beforeEach(module('myApp.viewZoo'));

  describe('viewZoo controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewZooCtrl = $controller('ViewZooCtrl');
      expect(viewZooCtrl).toBeDefined();
    }));

  });
});