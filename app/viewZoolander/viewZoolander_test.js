'use strict';

describe('myApp.viewZoolander module', function() {

  beforeEach(module('myApp.viewZoolander'));

  describe('viewZoolander controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewZoolanderCtrl = $controller('ViewZoolanderCtrl');
      expect(viewZoolanderCtrl).toBeDefined();
    }));

  });
});